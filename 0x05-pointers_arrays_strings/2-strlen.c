#include <string.h>

/**
 *_strlen - return the lenght of the string.
 * @s : the string to find it's lenght.
 * Return: return the lenght of the string.
*/
int _strlen(char *s)
{
	return (strlen(s));
}
