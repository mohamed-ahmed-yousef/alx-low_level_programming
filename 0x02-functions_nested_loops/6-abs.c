#include "main.h"
/**
* _abs - find if the char is lower or upper.
*
*@c: is an integer value passed from main function.
* Return: Always 0
*
*/
int _abs(int c)
{
	if (c > 0)
	return (c);
	else if (c < 0)
	return (-c);
	else
	return (c); }
